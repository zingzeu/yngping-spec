<div class="toolbar" role="navigation">
{{> nav-toggle}}
  {{#if site.homeUrl}}
  <a href="{{relativize site.homeUrl}}" class="home-link{{#if page.home}} is-current{{/if}}"></a>
  {{/if}}
{{> breadcrumbs}}
{{> page-versions}}
  {{#if (and page.fileUri (not env.CI))}}
  <div class="edit-this-page"><a href="{{page.fileUri}}">编辑此页</a></div>
  {{else if (and page.editUrl (not page.origin.private))}}
  <div class="edit-this-page"><a href="{{page.editUrl}}">编辑此页</a></div>
  {{/if}}
</div>
